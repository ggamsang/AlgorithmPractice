#include <iostream>
#include <cmath>

int advertise(int day)
{
  int like = 0;
  for(int i=0; i<day;i++)
    {
      like = floor(m/2)*3;
    }
  return like;
}
int main(int argc, char* argv[])
{
  int n;
  std::cin >> n;
  std::cout << advertise(n) << std::endl;
  return 0;
}
