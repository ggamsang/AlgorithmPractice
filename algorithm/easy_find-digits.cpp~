#include <iostream>

int findDigits(int n){
  int cnt=0, N = n;
  while(1){
    if(N==0)break;
    if((N%10)%n==0)cnt++;
    N=(int)(N/10);
  }
  return cnt;
}

int main(){
  int t;
  std::cin >> t;
  while(t--){
    int num;
    std::cin >> num;
    std::cout << findDigits(num) << std::endl;
  }
  return 0;
}
